# SPDX-FileCopyrightText: 2024 - 2025 Slavi Pantaleev
# SPDX-FileCopyrightText: 2024 Nikita Chernyi
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
- name: Ensure exim-relay paths exist
  ansible.builtin.file:
    path: "{{ item.path }}"
    state: directory
    mode: "0750"
    owner: "{{ exim_relay_uid }}"
    group: "{{ exim_relay_gid }}"
  with_items:
    - { path: "{{ exim_relay_base_path }}", when: true }
    - { path: "{{ exim_relay_container_image_self_build_src_files_path }}", when: "{{ exim_relay_container_image_self_build }}" }
  when: item.when | bool

- name: Ensure exim-relay spool path exists
  ansible.builtin.file:
    path: "{{ exim_relay_spool_path }}"
    state: directory
    mode: "0750"
    owner: "{{ exim_relay_container_user_uid }}"
    group: "{{ exim_relay_container_user_gid }}"
    recurse: true

- name: Ensure exim-relay environment variables file created
  ansible.builtin.template:
    src: "{{ role_path }}/templates/env.j2"
    dest: "{{ exim_relay_base_path }}/env"
    mode: "0640"
  register: exim_relay_config_result

- name: Ensure exim-relay DKIM private key file created
  ansible.builtin.copy:
    content: "{{ exim_relay_dkim_privkey_contents }}"
    dest: "{{ exim_relay_dkim_privkey_path }}"
    mode: "0640"
    owner: "{{ exim_relay_container_user_uid }}"
    group: "{{ exim_relay_container_user_gid }}"
  when: exim_relay_dkim_privkey_contents != "" and not exim_relay_relay_use|bool
  register: exim_relay_dkim_result

- name: Run if self-building of exim-relay container image is not enabled
  when: "not exim_relay_container_image_self_build | bool"
  block:
    - name: Ensure exim-relay container image is pulled via community.docker.docker_image
      when: devture_systemd_docker_base_container_image_pull_method == 'ansible-module'
      community.docker.docker_image:
        name: "{{ exim_relay_container_image }}"
        source: "{{ 'pull' if ansible_version.major > 2 or ansible_version.minor > 7 else omit }}"
        force_source: "{{ exim_relay_container_image_force_pull if ansible_version.major > 2 or ansible_version.minor >= 8 else omit }}"
        force: "{{ omit if ansible_version.major > 2 or ansible_version.minor >= 8 else exim_relay_container_image_force_pull }}"
      register: exim_relay_container_image_pull_result_ansible_module
      retries: "{{ devture_playbook_help_container_retries_count }}"
      delay: "{{ devture_playbook_help_container_retries_delay }}"
      until: exim_relay_container_image_pull_result_ansible_module is not failed

    - name: Ensure exim-relay container image is pulled via ansible.builtin.command
      when: devture_systemd_docker_base_container_image_pull_method == 'command'
      ansible.builtin.command:
        cmd: "{{ devture_systemd_docker_base_host_command_docker }} pull {{ exim_relay_container_image }}"
      register: exim_relay_container_image_pull_result_command
      retries: "{{ devture_playbook_help_container_retries_count }}"
      delay: "{{ devture_playbook_help_container_retries_delay }}"
      until: exim_relay_container_image_pull_result_command is not failed
      changed_when: "'Downloaded newer image' in exim_relay_container_image_pull_result_command.stdout"

- name: Run if self-building of exim-relay container image is enabled
  when: "exim_relay_container_image_self_build | bool"
  block:
    - name: Ensure exim-relay repository is present on self-build
      ansible.builtin.git:
        repo: "{{ exim_relay_container_image_self_build_repo }}"
        version: "{{ exim_relay_container_image_self_build_repo_version }}"
        dest: "{{ exim_relay_container_image_self_build_src_files_path }}"
        force: "yes"
      become: true
      become_user: "{{ exim_relay_uid }}"
      register: exim_relay_git_pull_results

    - name: Ensure exim-relay container image is built
      community.docker.docker_image:
        name: "{{ exim_relay_container_image }}"
        source: build
        force_source: "{{ exim_relay_git_pull_results.changed if ansible_version.major > 2 or ansible_version.minor >= 8 else omit }}"
        force: "{{ omit if ansible_version.major > 2 or ansible_version.minor >= 8 else exim_relay_git_pull_results.changed }}"
        build:
          dockerfile: Dockerfile
          path: "{{ exim_relay_container_image_self_build_src_files_path }}"
          pull: true
      register: exim_relay_container_image_build_result

- name: Ensure exim-relay container network is created via community.docker.docker_network
  when: devture_systemd_docker_base_container_network_creation_method == 'ansible-module'
  community.docker.docker_network:
    enable_ipv6: "{{ devture_systemd_docker_base_ipv6_enabled }}"
    name: "{{ exim_relay_container_network }}"
    driver: bridge
    driver_options: "{{ devture_systemd_docker_base_container_networks_driver_options }}"
  register: exim_relay_network_creation_result

- name: Ensure exim-relay container network is created via ansible.builtin.command
  when: devture_systemd_docker_base_container_network_creation_method == 'command'
  ansible.builtin.command:
    cmd: >-
      {{ devture_systemd_docker_base_host_command_docker }} network create
      {% if devture_systemd_docker_base_ipv6_enabled %}--ipv6{% endif %}
      {{ devture_systemd_docker_base_container_networks_driver_options_string }}
      {{ exim_relay_container_network }}
  register: exim_relay_network_creation_result
  changed_when: exim_relay_network_creation_result.rc == 0
  failed_when: exim_relay_network_creation_result.rc != 0 and 'already exists' not in exim_relay_network_creation_result.stderr

- name: Ensure exim-relay systemd service is present
  ansible.builtin.template:
    src: "{{ role_path }}/templates/systemd/exim-relay.service.j2"
    dest: "{{ devture_systemd_docker_base_systemd_path }}/{{ exim_relay_identifier }}.service"
    mode: "0644"
  register: exim_relay_systemd_service_result

- name: Determine whether exim-relay needs a restart
  ansible.builtin.set_fact:
    exim_relay_restart_necessary: >-
      {{
        exim_relay_config_result.changed | default(false)
        or exim_relay_dkim_result.changed | default(false)
        or exim_relay_systemd_service_result.changed | default(false)
        or exim_relay_container_image_pull_result_ansible_module.changed | default(false)
        or exim_relay_container_image_pull_result_command.changed | default(false)
        or exim_relay_container_image_build_result.changed | default(false)
      }}
